<script type="text/javascript" language="javascript">

  $("document").ready(function(){

    $("div#slide-area").jContent({orientation: 'vertical',
      width: 350,
//      width: 361,
//      height: 270,
      easing: "easeOutCirc",
      duration: 2000,
      auto: true,
      pause_on_hover: true,
      direction: 'next',
      pause: 5000});

  });

  function setId(id){
    $("#doc_id").val(id);
    $.ajax({
      url: "documents/"+id+"/keywords"
    })
        .done(function( data ) {
          alert(data);
        });
  }
</script>
