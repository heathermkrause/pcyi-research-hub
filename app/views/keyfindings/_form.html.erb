<%= form_for(@keyfinding) do |f| %>
  <% if @keyfinding.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@keyfinding.errors.count, "error") %> prohibited this keyfinding from being saved:</h2>

      <ul>
      <% @keyfinding.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :document_id %><br />
    <%= f.number_field :document_id %>
  </div>
  <div class="field">
    <%= f.label :keyfinding_text %><br />
    <%= f.text_area :keyfinding_text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
